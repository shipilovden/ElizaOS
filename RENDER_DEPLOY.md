# 🚀 Деплой на Render.com

Полное руководство по деплою ElizaOS агента на Render.com (бесплатно без карты).

## 📋 Содержание

- [Быстрый старт](#-быстрый-старт)
- [Настройка через Dashboard](#-настройка-через-dashboard)
- [Деплой через терминал](#-деплой-через-терминал)
- [Устранение проблем](#-устранение-проблем)
- [Конфигурация](#-конфигурация)

---

## 🏁 Быстрый старт

### ⚠️ Важно: API ключ нужен в двух местах

**API ключ OpenAI нужно указать:**
1. **Локально** - для разработки и тестирования (файл `.env` в `metasiberian-agent/`)
2. **На Render** - для продакшена (Environment Variables в Dashboard)

### Локальная настройка (для разработки)

1. Создайте файл `.env` в директории `metasiberian-agent/`:
   ```bash
   cd metasiberian-agent
   ```

2. Добавьте в файл `.env`:
   ```env
   OPENAI_API_KEY=sk-proj-AIcVdQx68yQmFkvbXkGAmYndNWsdTqQ0JN2JnseUoG1La
   NODE_ENV=development
   PORT=3000
   SERVER_PORT=3000
   ```

3. Теперь можно запускать локально:
   ```bash
   bun run start
   # или
   elizaos start
   ```

⚠️ **Безопасность**: Файл `.env` находится в `.gitignore` и не коммитится в репозиторий.

---

### Шаг 1: Регистрация на Render

1. Откройте: https://render.com
2. Нажмите **"Get Started for Free"**
3. Зарегистрируйтесь через GitHub
4. **НЕ требуется карта!**

### Шаг 2: Создание Web Service

1. В Render Dashboard нажмите **"New +"**
2. Выберите **"Web Service"**
3. Подключите GitHub (если еще не подключен)
4. Выберите репозиторий: `shipilovden/AiChat`
5. Нажмите **"Connect"**

### Шаг 3: Настройка сервиса

Заполните форму:

- **Name**: `metasiberian-agent` (или любое другое)
- **Region**: выберите ближайший (например, `Frankfurt` для Европы)
- **Branch**: `main`
- **Root Directory**: `metasiberian-agent` ⚠️ **ВАЖНО!**
- **Runtime**: `Node` (не Docker!)
- **Node Version**: `23.x`
- **Build Command**: 
  ```bash
  curl -fsSL https://bun.sh/install | bash && export PATH="$HOME/.bun/bin:$PATH" && bun install && bun run build
  ```
- **Start Command**: 
  ```bash
  export PATH="$HOME/.bun/bin:$PATH" && bun run start
  ```
- **Instance Type**: `Free` (по умолчанию)

### Шаг 4: Переменные окружения

Прокрутите вниз до **"Environment Variables"** и добавьте:

```
OPENAI_API_KEY=sk-proj-AIcVdQx68yQmFkvbXkGAmYndNWsdTqQ0JN2JnseUoG1La
NODE_ENV=production
PORT=3000
SERVER_PORT=3000
```

⚠️ **Важно**: `OPENAI_API_KEY` нужно установить вручную в Dashboard, так как в `render.yaml` он помечен как `sync: false` для безопасности.

### Шаг 5: Деплой

1. Нажмите **"Create Web Service"**
2. Render начнет деплой (займет ~5-10 минут)
3. После завершения вы получите URL: `https://metasiberian-agent.onrender.com`

### ✅ Проверка работы

После деплоя проверьте:
- API: `https://metasiberian-agent.onrender.com/api/server/ping`
- Web UI: `https://metasiberian-agent.onrender.com`

---

## 🖥️ Деплой через терминал

Render автоматически деплоит последний коммит из ветки `main` при каждом пуше в GitHub.

### Быстрый деплой

#### Linux/macOS (Bash)

```bash
# Простой деплой с автоматическим сообщением
./scripts/deploy-render.sh

# Деплой с кастомным сообщением коммита
./scripts/deploy-render.sh "Обновление функционала агента"
```

#### Windows (PowerShell)

```powershell
# Простой деплой
.\scripts\deploy-render.ps1

# Деплой с кастомным сообщением
.\scripts\deploy-render.ps1 -CommitMessage "Обновление функционала агента"
```

### Ручной деплой через Git

Если скрипты не работают:

```bash
# 1. Убедитесь что все изменения закоммичены
git status

# 2. Добавьте изменения (если есть)
git add .

# 3. Создайте коммит
git commit -m "Deploy to Render"

# 4. Запушьте в GitHub
git push origin main
```

Render автоматически обнаружит новый коммит и начнет деплой.

### Настройка автоматического деплоя

Render по умолчанию настроен на автоматический деплой при каждом пуше в ветку `main`:

1. Откройте ваш сервис в Render Dashboard
2. Перейдите в **Settings** → **Build & Deploy**
3. Убедитесь что **Auto-Deploy** включен
4. Проверьте что **Branch** установлен на `main`

### Проверка статуса деплоя

1. Откройте: https://dashboard.render.com
2. Выберите ваш сервис `metasiberian-agent`
3. Перейдите в раздел **Logs** для просмотра процесса деплоя
4. Проверьте статус в разделе **Events**

---

## 🔧 Устранение проблем

### 1. ❌ Ошибка: "bun: command not found"

**Причина**: Bun не установлен в окружении Render

**Решение**: 
- Убедитесь что в `render.yaml` есть установка Bun в `buildCommand`
- Проверьте что используется `env: node` (не Docker)
- В Render Dashboard: **Settings** → **Build & Deploy** → **Environment** = `Node`

### 2. ❌ Ошибка: "Cannot find module '@elizaos/core'"

**Причина**: Зависимости не установлены или неправильная структура проекта

**Решение**:
1. Убедитесь что `rootDir: metasiberian-agent` установлен в `render.yaml`
2. Проверьте что `package.json` в `metasiberian-agent` содержит все зависимости
3. Очистите build cache: **Settings** → **Clear build cache** → **Clear cache**

### 3. ❌ Ошибка: "Port already in use" или "EADDRINUSE"

**Причина**: Неправильная настройка порта

**Решение**:
1. Убедитесь что в переменных окружения установлено:
   - `PORT=3000`
   - `SERVER_PORT=3000`
2. Render автоматически назначает порт через переменную `PORT`
3. Не указывайте порт явно в коде - используйте `process.env.PORT`

### 4. ❌ Ошибка: "Build failed" или "Build timeout"

**Причина**: Слишком долгая сборка или ошибки в процессе сборки

**Решение**:
1. Проверьте логи сборки в разделе **Logs**
2. Убедитесь что все зависимости указаны в `package.json`
3. Проверьте что `bun run build` выполняется успешно локально:
   ```bash
   cd metasiberian-agent
   bun install
   bun run build
   ```

### 5. ❌ Ошибка: "Environment variable not found"

**Причина**: Отсутствуют необходимые переменные окружения

**Решение**:
1. В Render Dashboard: **Settings** → **Environment Variables**
2. Убедитесь что добавлены:
   - `NODE_ENV=production`
   - `PORT=3000`
   - `SERVER_PORT=3000`
   - `OPENAI_API_KEY=<ваш ключ>` (установите вручную в Dashboard)

### 6. ❌ Ошибка: "Root directory not found"

**Причина**: Неправильно указан `rootDir` в `render.yaml`

**Решение**:
1. Убедитесь что `rootDir: metasiberian-agent` указан в `render.yaml`
2. Проверьте что директория `metasiberian-agent` существует в репозитории
3. В Render Dashboard: **Settings** → **Build & Deploy** → **Root Directory** = `metasiberian-agent`

### 7. ❌ Ошибка: "Docker detected" (но вы хотите использовать Node)

**Причина**: Render автоматически определил Docker из-за наличия Dockerfile

**Решение**:
1. В Render Dashboard: **Settings** → **Build & Deploy**
2. **ИЗМЕНИТЕ** **Environment** с `Docker` на `Node`
3. Установите **Node Version**: `23.x`
4. Сохраните изменения
5. Render автоматически перезапустит деплой

### Быстрое исправление

Если деплой не работает:

1. **Очистите build cache**: Settings → Clear build cache → Clear cache
2. **Проверьте настройки**:
   - Environment = `Node`
   - Node Version = `23.x`
   - Root Directory = `metasiberian-agent`
3. **Перезапустите деплой**: Manual Deploy → Deploy latest commit
4. **Проверьте логи**: Logs → Build Logs

### Что искать в логах

✅ **Успешная установка Bun**:
```
Installing Bun...
✓ Bun installed successfully
bun --version
1.2.21
```

✅ **Успешная установка зависимостей**:
```
bun install
...
✓ Installed X packages
```

✅ **Успешная сборка**:
```
bun run build
🚀 Building project...
✓ Built X file(s) - X.XXMB
✅ Build complete!
```

✅ **Успешный запуск**:
```
bun run start
Server started on port 3000
```

---

## ⚙️ Конфигурация

### Файл render.yaml

Проект использует `render.yaml` для автоматической настройки деплоя. Убедитесь что файл выглядит так:

```yaml
services:
  - type: web
    name: metasiberian-agent
    env: node
    nodeVersion: 23.x
    rootDir: metasiberian-agent
    buildCommand: |
      curl -fsSL https://bun.sh/install | bash
      export PATH="$HOME/.bun/bin:$PATH"
      bun --version
      bun install
      bun run build
    startCommand: |
      export PATH="$HOME/.bun/bin:$PATH"
      bun run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: SERVER_PORT
        value: 3000
      - key: OPENAI_API_KEY
        sync: false  # Установите вручную в Dashboard
```

### Чек-лист перед деплоем

- [ ] `render.yaml` содержит правильные команды
- [ ] `rootDir: metasiberian-agent` указан
- [ ] `env: node` установлен (не Docker)
- [ ] `nodeVersion: 23.x` указан
- [ ] Build Command содержит установку Bun
- [ ] Start Command использует Bun
- [ ] Все переменные окружения добавлены в Dashboard
- [ ] `OPENAI_API_KEY` установлен вручную в Dashboard
- [ ] Проект собирается локально: `cd metasiberian-agent && bun install && bun run build`
- [ ] Проект запускается локально: `cd metasiberian-agent && bun run start`

---

## ⚠️ Важные замечания

### "Засыпание" приложения

На бесплатном плане Render приложение "засыпает" после 15 минут бездействия:
- Первый запрос после сна займет ~30 секунд
- Последующие запросы работают нормально
- Это нормально для тестирования

### Если нужно постоянное подключение

Для постоянной работы (без "сна") можно:
1. Использовать бесплатный сервис для "пробуждения" (например, UptimeRobot)
2. Или перейти на платный план ($7/месяц)

### Автоматический деплой

- Render автоматически деплоит каждый коммит в `main`
- Время деплоя: обычно 5-10 минут
- Всегда проверяйте логи деплоя в Render Dashboard при ошибках

---

## 📚 Полезные ссылки

- **Render Dashboard**: https://dashboard.render.com
- **Документация Render**: https://render.com/docs
- **Render API Reference**: https://render.com/docs/api
- **Логи деплоя**: Dashboard → ваш сервис → Logs

---

## 📞 Если ничего не помогает

1. Проверьте что последняя версия `render.yaml` закоммичена и запушена в репозиторий
2. Убедитесь что в Render Dashboard используются настройки из `render.yaml` (или настройте вручную)
3. Создайте новый сервис с нуля, используя правильные настройки
4. Обратитесь в поддержку Render с логами деплоя
