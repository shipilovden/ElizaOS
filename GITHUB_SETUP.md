# Настройка GitHub репозитория для Railway

## 📋 Шаги для создания репозитория

### 1. Создайте репозиторий на GitHub

1. Перейдите на https://github.com/new
2. Название репозитория: `ElizaOS` (или любое другое)
3. Выберите **Private** (чтобы не публиковать код публично)
4. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)
5. Нажмите "Create repository"

### 2. Подключите локальный репозиторий к GitHub

```bash
# Перейдите в директорию проекта
cd C:\ElizaOS

# Добавьте remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/ElizaOS.git

# Или если используете SSH:
# git remote add origin git@github.com:YOUR_USERNAME/ElizaOS.git

# Проверьте что remote добавлен
git remote -v
```

### 3. Добавьте файлы и создайте первый коммит

```bash
# Проверьте статус
git status

# Добавьте все файлы (кроме тех что в .gitignore)
git add .

# Создайте коммит
git commit -m "Initial commit: Metasiberian Agent for Railway deployment"

# Переименуйте ветку в main (если нужно)
git branch -M main

# Запушьте в GitHub
git push -u origin main
```

### 4. Проверьте что все загружено

Перейдите на https://github.com/YOUR_USERNAME/ElizaOS и убедитесь что:
- ✅ Все файлы загружены
- ✅ `.env` файл **НЕ** виден (он в .gitignore)
- ✅ `all_api_keys.txt` **НЕ** виден (он в .gitignore)

## ⚠️ Важно!

**НЕ КОММИТЬТЕ:**
- `.env` файлы
- `all_api_keys.txt`
- `node_modules/`
- Любые файлы с секретами

Все это уже добавлено в `.gitignore`.

## 🚀 Следующий шаг

После создания репозитория на GitHub, переходите к деплою на Railway:
см. `RAILWAY_DEPLOY.md`

